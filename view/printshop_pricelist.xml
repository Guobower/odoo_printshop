<?xml version="1.0" encoding="utf-8"?>

<odoo>
     <data>

		<record model="ir.actions.act_window" id="action_printshop_listsprice_tree_view">
			<field name="name">printshop listsprice</field>
			<field name="type">ir.actions.act_window</field>
			<field name="res_model">printshop.listsprice</field>
			<field name="view_type">form</field>
			<field name="view_mode">tree,form</field>
		</record>

		<menuitem id="printshop_pricelist_menu" parent="offset_conf_menu"
			action="action_printshop_listsprice_tree_view" />

            

    </data>
</odoo>